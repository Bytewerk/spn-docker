git clone ..
git submodule init
git submodule update

cd gameserver/code
git pull
git checkout feature/mysql2
git submodule init
git submodule update

cd relayserver/code
git pull
git checkout tech/json
git submodule init
git submodule update

cd website/code
git pull
git checkout branch/beautify_views

cd vis/code
git pull
git checkout feature/JsonProtocol

# build images
docker build -t gpn-snake:gameserver-1.0.0 -f gameserver/Dockerfile .
docker build -t gpn-snake:relayserver-1.0.0 -f relayserver/Dockerfile .
docker build -t gpn-snake:website-1.0.0 -f website/Dockerfile .

# start docker compose - complete
docker-compose -f docker-compose.yml up

# or one by one, because mysql is quite slow and gameserver need it to be online
docker-compose -f docker-compose.yml up -d database
docker-compose -f docker-compose.yml up -d website
docker-compose -f docker-compose.yml up -d gameserver
docker-compose -f docker-compose.yml up -d relayserver
docker-compose -f docker-compose.yml up -d proxy